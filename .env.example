# Required: OpenAI API key for scoring, enrichment, and embeddings
OPENAI_API_KEY=sk-...

# Required: GitHub token for fetching repositories
GITHUB_TOKEN=ghp_...

# Optional: Path to existing posts.db for import (step 1)
# IMPORT_DB_PATH=/path/to/existing/posts.db
